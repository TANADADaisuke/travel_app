import { formHandler } from '../src/client/js/app';

describe("Testing the submit functionality", () => {
    test("Testing the formHander() function", () => {
        expect(formHandler).toBeDefined();
    })
});
